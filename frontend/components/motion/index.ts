/**
 * Motion-примитивы (Фаза 5). Переносят CSS `@keyframes` прототипа
 * (`design-reference.html:20–23`) на Reanimated 4 — по фактическим
 * длительностям из дизайн-референса. Каждый уважает системный флаг
 * «уменьшить движение» (`useReducedMotion`).
 */

export { Blink } from './Blink';
export { Pulse } from './Pulse';
export { SlideUp } from './SlideUp';
export { Spin } from './Spin';
