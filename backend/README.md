# AcademicSpace — backend

Заготовка. Наполняется в Фазе 8.1 (Flask bootstrap + инфраструктура) — см.
`docs/00-roadmap.md` и план бекенда.

Стек (зафиксирован, ещё не реализован): Flask + Flask-SQLAlchemy + Flask-Migrate +
Flask-JWT-Extended + Flask-CORS + marshmallow, PostgreSQL, пакетный менеджер `uv`.
